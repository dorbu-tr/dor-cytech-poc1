# CyTech HLD Gate - New Service Plan

> **[SHADOW MODE]** This HLD was generated by the Security Agent in shadow mode.
> The verdict is advisory only. Human validation is required.

---

## Service Metadata

| Field | Value |
|-------|-------|
| **Service Name** (mandatory) | `dor-cytech-poc1` (Trading Positions API) |
| **Service Repository** (mandatory) | `dor-cytech-poc1` |
| **Domain** (mandatory) | Trading |
| **Owner (R&D team)** (mandatory) | Trading Team (eToro Trading NuGets) |
| **Author** (mandatory) | `<to be confirmed by developer>` |
| **Target Environments** (mandatory) | [x] QA  [x] INT  [x] STG  [ ] DEMO  [ ] REAL  [x] PROD |
| **Main Technology** (mandatory) | .NET 8 (C#) |
| **Service Type** (mandatory) | API (Aggregator / Wrapper-proxy) |

---

## 1. Architecture Overview

**Reviewers: IT, Security, SRE**

### 1.1 Service Classification (mandatory)

| Field | Value |
|-------|-------|
| Classification | `public-facing` (STS client-facing endpoints) |
| AKS Service | Yes |

### 1.2 System Diagram (mandatory)

Link to draw.io / Confluence diagram: `<TBD - to be created during implementation>`

The diagram must show:
- Service classification: public-facing aggregator
- AKS service placement: Trading cluster
- All external dependencies: Positions API, Instruments API

### 1.3 External Dependencies

| Dependency | Type | Purpose |
|-----------|------|---------|
| Positions API | Service (Internal REST API) | Upstream source for trading position data (list, detail, close operations) |
| Instruments API | Service (Internal REST API) | Upstream source for instrument metadata enrichment (name, type, active status) |

---

## 2. Deployment

**Reviewers: DevOps, SRE**

| Field | Value |
|-------|-------|
| **Platform** | AKS |
| **Health Endpoint** (mandatory) | `/healthcheck` |
| **High Availability Required** (mandatory) | Yes (PROD) |

### 2.1 Cluster Assignments

| Environment | Cluster |
|------------|---------|
| QA | Trading (auto-resolve) |
| INT | Trading (auto-resolve) |
| STG | Trading (auto-resolve) |
| PROD | Trading |

> Cluster derived from: Domain = Trading → Trading cluster (cluster-deployment skill, first-match rule).
> Validated in security-review.md — plan declares Trading, matches derived recommendation.

---

## 3. Scalability & Reliability

**Reviewer: SRE**

### 3.1 Autoscaling (HPA)

| Resource | Request | Limit |
|----------|---------|-------|
| **CPU** (mandatory) | `<TBD - to be filled during implementation>` | `<TBD>` |
| **Memory** (mandatory) | `<TBD - to be filled during implementation>` | `<TBD>` |

### 3.2 Replica Configuration

| Field | Value |
|-------|-------|
| **Min Replicas** (mandatory) | `<TBD - to be filled during implementation>` |
| **Max Replicas** (mandatory) | `<TBD - to be filled during implementation>` |

### 3.3 Multi-Region

| Field | Value |
|-------|-------|
| **Supported Regions** (mandatory) | `<TBD - to be filled during implementation>` |

---

## 4. Dependencies / Network

**Reviewers: IT, Security, SRE**

### 4.1 Network Rules

| # | Source (exact Azure name) | Destination (exact Azure name) | Protocol / Port | Environments | Security Architect Approval |
|---|--------------------------|-------------------------------|----------------|-------------|---------------------------|
| 1 | `dor-cytech-poc1.trading-cluster` | Positions API | HTTPS / 443 | QA, INT, STG, PROD | `<TBD>` |
| 2 | `dor-cytech-poc1.trading-cluster` | Instruments API | HTTPS / 443 | QA, INT, STG, PROD | `<TBD>` |

> **Note**: Exact Azure resource names to be confirmed during implementation. Source and destination
> names above are logical placeholders.

---

## 5. Security

**Reviewer: Security**

### 5.1 Data Classification (mandatory)

| # | Field | Value | Result | Rule Applied | Evidence |
|---|-------|-------|--------|-------------|----------|
| D1 | **Third-Party Interaction** | No | PASS | No third-party APIs involved | Both upstream APIs (Positions, Instruments) are internal eToro services |
| D2 | **Third-Party Due Diligence Approved** (if D1=Yes) | N/A | N/A | D1=No, not applicable | — |
| D3 | **Customer / User Identifiers** | Yes — internal user ID from STS token | PASS | Internal IDs only, no PII identifiers (no email, name, phone) | User identity extracted from STS token as internal ID; never stored or logged |
| D4 | **Rate Limit (requests/second)** | `<TBD - to be configured>` | PASS | Must be numeric; to be set during implementation | Rate limiting to be configured via CCM |
| D5 | **PII Level** | None | PASS | None=PASS | Plan declares PII Handling = None; no PII stored, processed, or logged |
| D6 | **Financial / Trading Data** | Read-only (positions data) + Write (close action) | REVIEW | Read-write trading data on Trading cluster — elevated review advisory | Service reads position data and initiates close actions via upstream Positions API; Trading cluster is correct placement |
| D7 | **Authentication / Authorization Artifacts** | Access tokens (STS client-facing) + Service identities (API Key for upstream) | PASS | Auth artifacts declared and documented | STS tokens for client auth; API Keys (from KeyVault) for upstream service auth |
| D8 | **Configuration System** | CCM (etcd) + KeyVault + SCB | PASS | Configuration system declared | Plan confirms Trading CCM/KeyVault/SCB pattern per constitution-team.md |

### 5.2 Security Criteria Evaluation

| # | Criterion | Source Field in plan.md | Value | Result | Consequence if FAIL | Evidence |
|---|-----------|----------------------|-------|--------|-------------------|----------|
| A1 | Data classification = Public or Internal | Data Classification | Internal | PASS | Blocks auto-approve; Confidential→manual, Restricted→reject if unauth | Declared Internal; handles trading IDs, rates, instrument metadata — no PII, no credentials |
| A2 | No direct eToroDB access | eToroDB Access | No | PASS | Blocks auto-approve | Aggregator pattern; no SQL packages in NuGets checklist; no DB access |
| A3 | No PII storage/processing | PII Handling | None | PASS | Blocks auto-approve | Users referenced by internal ID from STS token only; no PII stored, logged, or processed |
| A4 | No sensitive dependencies | Sensitive Dependencies | None | PASS | Blocks auto-approve | Positions API and Instruments API are internal services, not PCI/financial secrets |
| A5 | Auth present on private endpoints | Authentication Model + External-Facing Endpoints | STS on all 3 endpoints | PASS | Blocks auto-approve; missing auth→R4 reject | STS auth globally applied via OpenAPI `stsAuth` security scheme |
| A6 | No PII/secrets in logs | Plan logging declarations | Framework.Log, no PII | PASS | Blocks auto-approve; PII without masking→R1 reject | Plan establishes logging discipline: "no PII stored, logged, or processed" |
| A7 | Cluster placement matches service type | Target Cluster vs Q1-Q4 recommendation | Trading = Trading | PASS | Blocks auto-approve; less restrictive→R12 reject | Trading domain → Trading cluster; plan declares Trading; match confirmed |
| A8 | Service matches qualifying type | Service Type | Wrapper-proxy | PASS | Blocks auto-approve; type=Other→manual review | Wrapper-proxy (aggregator of two internal APIs) is a qualifying type |
| R1 | PII in logs without masking | Plan logging declarations | No PII logged | PASS | **Auto-reject** | Plan confirms "no PII stored, logged, or processed"; Framework.Log with structured logging |
| R2 | Hardcoded secrets/credentials | Plan text scan | None found | PASS | **Auto-reject** | No API keys, passwords, tokens, or connection strings in plan text |
| R3 | Secrets embedded in plan text | Plan text scan | None found | PASS | **Auto-reject** | No active secrets in plan text |
| R4 | Sensitive endpoint without auth | External-Facing Endpoints + Auth Model | All authenticated | PASS | **Auto-reject** | All three endpoints require STS token; OpenAPI spec enforces `stsAuth` globally |
| R5 | Plan/spec internal contradictions | Cross-reference plan.md vs spec.md | **Contradicts** | **FAIL** | **Auto-reject** | spec.md NFR-010 says "Front cluster (31)" but plan.md says "Trading cluster". Plan is correct per cluster-deployment rules; spec needs updating. |
| R6 | Data classification mismatch | Data Classification vs data access | Consistent | PASS | **Auto-reject** | Internal classification matches: trading position IDs, rates, amounts — all Internal-level |
| R7 | Known vulnerable dependencies | Declared dependencies | No known CVEs | PASS | **Auto-reject** | Standard eToro Trading NuGets + mainstream packages (Polly, FluentValidation, AutoMapper) |
| R8 | Hardcoded IPs/hostnames/connection strings | Plan text scan | **IP found** | **FAIL** | **Auto-reject** | Plan Phase 3: `http://10.0.5.120:8080/healthcheck` — hardcoded IP must be in CCM/KeyVault |
| R9 | Overly permissive IAM/RBAC | Plan IAM/permission declarations | None declared | PASS | **Auto-reject** | No wildcard permissions or overly permissive roles |
| R10 | Sensitive data in error responses | Plan error handling declarations | Standard handling | PASS | **Auto-reject** | Template-provided ExceptionHandlingMiddleware with `application/problem+json` |
| R11 | Missing input validation | Endpoint declarations | Validation declared | PASS | **Auto-reject** | FluentValidation for all three request types; ClosePositionBody has enum validation |
| R12 | Cluster placement mismatch | Target Cluster vs recommendation | Match | PASS | **Auto-reject** | Trading domain → Trading cluster; plan declares Trading; derived matches declared |

### 5.3 Manual Review Triggers

| # | Trigger | Source Field in plan.md | Detected | Evidence |
|---|---------|----------------------|----------|----------|
| M1 | PCI scope | PCI Scope | NO | PCI Scope = No |
| M2 | Auth/authz module changes | Service description in plan | NO | Uses template-provided auth (AppSecretAuthenticationMiddleware, STS `[Authentication]`); no auth module created or modified |
| M3 | Unclear or mixed data classification | Data Classification | NO | Clearly declared as Internal with full justification |
| M4 | Trading or Money cluster | Target Cluster / Q1 answer | **YES** | Target Cluster = Trading; Trading cluster services require elevated review |
| M5 | Ambiguous external-facing endpoints | External-Facing Endpoints | NO | All three endpoints clearly documented with data sensitivity (Internal data only) |
| M6 | Confidential data classification | Data Classification | NO | Data Classification = Internal, not Confidential |
| M7 | Restricted data with authenticated access | Data Classification + Auth Model | NO | No Restricted data involved |
| M8 | Cluster-deployment skill conflict | Q1-Q4 vs plan declared cluster | NO | No conflict; Trading domain → Trading cluster, plan declares Trading |
| DYN-M-1 | Security Profile describes close `reason` as "free-text string, up to 500 chars" but data model defines it as CloseReason enum | Security Profile vs data-model.md | INFO | Minor inconsistency; validation is declared regardless. Recommend aligning the description. |

> **Verdict derivation**:
> - R5 FAIL + R8 FAIL in 5.2 → **Auto-reject**
> - M4 TRIGGERED in 5.3 → Would be Manual review (superseded by Auto-reject)
> - All A-rows PASS in 5.2

---

## 6. Monitoring & Observability

**Reviewer: SRE**

### 6.1 Logging (mandatory)

| Field | Value |
|-------|-------|
| **Log Retention** | `<TBD - to be configured>` |
| **Sourcetype** | `<TBD - to be configured>` |
| **PII Redaction Confirmed** | Yes — No PII (matches Section 5 D5: PII Level = None) |

### 6.2 Custom Alerts (mandatory - at least one)

| Alert Name | Metric | Threshold | Severity |
|-----------|--------|-----------|----------|
| `positions-api-latency-p95` | GET endpoint p95 latency | > 150ms | P2 |
| `close-api-latency-p95` | POST close p95 latency | > 300ms | P2 |
| `upstream-circuit-breaker-open` | Circuit breaker state | Open | P1 |

### 6.3 Required Instrumentation

| Instrumentation | Implemented |
|----------------|-------------|
| **Tracing** | Planned (X-Request-Id, X-Correlation-Id) |
| **Metrics** | Planned (IMetrics from Framework.Metrics — Timer, Counter, Error counter per service method) |
| **Logs** | Planned (Framework.Log.ILogger with structured logging) |

---

## 7. Agent Decision Overrides

### 7.1 DevOps Agent

| Field | Value |
|-------|-------|
| Resolved Cluster - QA | Trading (auto) |
| Resolved Cluster - INT | Trading (auto) |
| Resolved Cluster - STG | Trading (auto) |
| Resolved Cluster - PROD | Trading |
| Assignment Rationale | Domain = Trading → Trading cluster (first-match rule in cluster-deployment skill) |

### 7.2 IT Agent

| Dependency # | Status | Action | Jira Ticket |
|-------------|--------|--------|-------------|
| 1 (Positions API) | pending | Verify network rule and API key provisioning | N/A |
| 2 (Instruments API) | pending | Verify network rule and API key provisioning | N/A |

### 7.3 Security Agent

| Field | Value |
|-------|-------|
| Decision | **Auto-reject** (Shadow Mode — advisory only) |
| Reason | R5: spec.md NFR-010 contradicts plan.md on cluster (Front vs Trading). R8: Hardcoded IP `10.0.5.120` in plan Phase 3. Additionally, M4 (Trading cluster) would trigger manual review after R5/R8 are resolved. |
| Reviewer (if manual) | Security architect (for M4 Trading cluster review after R5/R8 remediation) |

### 7.4 SRE Agent

| Field | Value |
|-------|-------|
| Decision | *(pending — to be evaluated)* |
| Monitoring Jira Task | N/A |
| Reason (if reject) | N/A |

---

## 8. Final Approvals

| Reviewer | Domain | Approved (Y/N) | Date | Comments |
|----------|--------|:--------------:|------|----------|
| Security | Security | N (Auto-reject — Shadow Mode advisory) | 2026-04-06 | R5 + R8 violations; M4 Trading cluster review pending |
| IT | | | | |
| SRE | | | | |

---

## Version History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 2026-04-06 | Security Agent (Shadow Mode) | Initial plan — Auto-reject verdict (R5 spec/plan cluster contradiction, R8 hardcoded IP) |
