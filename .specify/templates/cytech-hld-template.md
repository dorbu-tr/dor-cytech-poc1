# CyTech HLD Gate - New Service Plan

> This template is auto-generated by the SDD Plan step. It contains all the information
> required for the CyTech Agent Reviewer to perform automated validation across DevOps,
> IT, Security, and SRE domains.
>
> **Instructions**: Fill in all `<placeholder>` fields. Fields marked **(mandatory)** will
> trigger auto-reject if left blank. Fields marked **(conditional)** require justification
> if a non-standard value is chosen.

---

## Service Metadata

| Field | Value |
|-------|-------|
| **Service Name** (mandatory) | `<service-name>` |
| **Service Repository** (mandatory) | `<repo-url>` |
| **Domain** (mandatory) | `<domain>` |
| **Owner (R&D team)** (mandatory) | `<team>` |
| **Author** (mandatory) | `<author-name>` |
| **Target Environments** (mandatory) | `[ ] QA  [ ] INT  [ ] STG  [ ] DEMO  [ ] REAL  [ ] PROD` |
| **Main Technology** (mandatory) | `<.NET / Node / Python / other>` |
| **Service Type** (mandatory) | `<API / Worker / Cron>` |

> **Note**: If Domain = `wallet` or `pci`, a mandatory security review meeting is required
> regardless of other field values.

---

## 1. Architecture Overview

**Reviewers: IT, Security, SRE**

### 1.1 Service Classification (mandatory)

| Field | Value |
|-------|-------|
| Classification | `<public-facing / internal-backend / worker>` |
| AKS Service | `<Yes / No>` |

### 1.2 System Diagram (mandatory)

Link to draw.io / Confluence diagram: `<diagram-link>`

The diagram must show:
- Service classification (public / backend / worker)
- AKS service placement
- All external dependencies

### 1.3 External Dependencies

| Dependency | Type | Purpose |
|-----------|------|---------|
| `<resource-name>` | `<DB / Service Bus / Redis / Third-Party / Other>` | `<description>` |
| `<resource-name>` | `<type>` | `<description>` |

---

## 2. Deployment

**Reviewers: DevOps, SRE**

| Field | Value |
|-------|-------|
| **Platform** | AKS |
| **Health Endpoint** (mandatory) | `<path>` (e.g., `/healthcheck`) |
| **High Availability Required** (mandatory) | `<Yes / No>` |

### 2.1 Cluster Assignments

> Clusters can be left blank -- the DevOps Agent will auto-resolve based on
> service classification, HA requirements, and environment.

| Environment | Cluster |
|------------|---------|
| QA | `<cluster or auto>` |
| INT | `<cluster or auto>` |
| STG | `<cluster or auto>` |
| PROD | `<cluster or auto>` |

---

## 3. Scalability & Reliability

**Reviewer: SRE**

### 3.1 Autoscaling (HPA)

| Resource | Request | Limit |
|----------|---------|-------|
| **CPU** (mandatory) | `<value>` | `<value>` |
| **Memory** (mandatory) | `<value>` | `<value>` |

### 3.2 Replica Configuration

| Field | Value |
|-------|-------|
| **Min Replicas** (mandatory) | `<n>` |
| **Max Replicas** (mandatory) | `<n>` |

> **Validation Rule**: Min replicas must be > 1. If set to 1, provide justification:
>
> Justification (if min = 1): `<reason or N/A>`

### 3.3 Multi-Region

| Field | Value |
|-------|-------|
| **Supported Regions** (mandatory) | `<region-list>` |

> **Validation Rule**: Must support > 1 region. If single region, provide justification:
>
> Justification (if single region): `<reason or N/A>`

---

## 4. Dependencies / Network

**Reviewers: IT, Security, SRE**

> **Critical**: Every dependency must use the **exact Azure infrastructure resource name**
> as it appears in Azure. Missing or unclear resource names will trigger **auto-reject**.

### 4.1 Network Rules

| # | Source (exact Azure name) | Destination (exact Azure name) | Protocol / Port | Environments | Security Architect Approval |
|---|--------------------------|-------------------------------|----------------|-------------|---------------------------|
| 1 | `<source>` | `<destination>` | `<port or protocol>` | `<env-list>` | `<name>` / `<date>` |
| 2 | `<source>` | `<destination>` | `<port or protocol>` | `<env-list>` | `<name>` / `<date>` |

> Examples:
> - Source: `my-service.aks-prod-cluster` | Destination: `sts.api.prod` | Port: `443 (SSL)` | Envs: `INT, STG, PROD`
> - Source: `my-service.aks-prod-cluster` | Destination: `sql-server-prod.database.windows.net` | Port: `MSSQL` | Envs: `PROD`

---

## 5. Security

**Reviewer: Security**

### 5.1 Data Classification (mandatory)

| # | Field | Value | Result | Rule Applied | Evidence |
|---|-------|-------|--------|-------------|----------|
| D1 | **Third-Party Interaction** | `<Yes / No>` | | If Yes → due diligence must be approved, else auto-reject | |
| D2 | **Third-Party Due Diligence Approved** (if D1=Yes) | `<Yes / No / N/A>` | | Must be Yes if D1=Yes, else auto-reject | |
| D3 | **Customer / User Identifiers** | `<Yes / No>` | | If Yes → list identifier types | |
| D4 | **Rate Limit (requests/second)** (mandatory) | `<numeric value>` | | Must be numeric, must not be blank | |
| D5 | **PII Level** (mandatory) | `<None / Elevated / Yes>` | | None=PASS; Elevated=manual review; Yes=mandatory meeting | |
| D6 | **Financial / Trading Data** (mandatory) | `<None / Read-only / Read-write>` | | Read-write + non-Trading cluster = manual review | |
| D7 | **Authentication / Authorization Artifacts** (mandatory) | `<None / Access tokens / Service identities>` | | Must not be blank | |
| D8 | **Configuration System** (mandatory) | `<SCB / CCM (etcd) / App Config>` | | Must be declared | |

> **Validation Rules**:
> - PII = `Elevated` → manual security review required
> - PII = `Yes` → mandatory security review meeting
> - Third-party = `Yes` AND due diligence not approved → auto-reject
> - Domain = `wallet` or `pci` → mandatory review meeting regardless

### 5.2 Security Criteria Evaluation

| # | Criterion | Source Field in plan.md | Value | Result | Consequence if FAIL | Evidence |
|---|-----------|----------------------|-------|--------|-------------------|----------|
| A1 | Data classification = Public or Internal | Data Classification | | | Blocks auto-approve; Confidential→manual, Restricted→reject if unauth | |
| A2 | No direct eToroDB access | eToroDB Access | | | Blocks auto-approve | |
| A3 | No PII storage/processing | PII Handling | | | Blocks auto-approve | |
| A4 | No sensitive dependencies | Sensitive Dependencies | | | Blocks auto-approve | |
| A5 | Auth present on private endpoints | Authentication Model + External-Facing Endpoints | | | Blocks auto-approve; missing auth on sensitive endpoint→R4 reject | |
| A6 | No PII/secrets in logs | plan logging declarations | | | Blocks auto-approve; PII without masking→R1 reject | |
| A7 | Cluster placement matches service type | Target Cluster vs Q1-Q4 recommendation | | | Blocks auto-approve; less restrictive→R12 reject | |
| A8 | Service matches qualifying type | Service Type | | | Blocks auto-approve; type=Other→manual review | |
| R1 | PII in logs without masking | plan logging declarations | | | **Auto-reject** | |
| R4 | Sensitive endpoint without auth | External-Facing Endpoints + Authentication Model | | | **Auto-reject** | |
| R6 | Data classification mismatch | Data Classification vs actual data access patterns | | | **Auto-reject** | |
| R11 | Missing input validation | Endpoint declarations (file upload, free-text) | | | **Auto-reject** | |
| R12 | Cluster placement mismatch | Target Cluster vs cluster-deployment recommendation | | | **Auto-reject** | |

> **Dynamic rows**: At runtime, if the evaluator discovers additional signals
> from plan.md that don't map to a predefined row, add a new row with prefix
> `DYN-` and populate all columns.

### 5.3 Manual Review Triggers

| # | Trigger | Source Field in plan.md | Detected | Evidence |
|---|---------|----------------------|----------|----------|
| M1 | PCI scope | PCI Scope | | |
| M2 | Auth/authz module changes | Service description in plan | | |
| M3 | Unclear or mixed data classification | Data Classification | | |
| M4 | Trading or Money cluster | Target Cluster / Q1 answer | | |
| M5 | Ambiguous external-facing endpoints | External-Facing Endpoints | | |
| M6 | Confidential data classification | Data Classification | | |
| M7 | Restricted data with authenticated access | Data Classification + Auth Model | | |
| M8 | Cluster-deployment skill conflict | Q1-Q4 vs plan declared cluster | | |

> **Dynamic triggers**: Runtime-discovered triggers are added with `DYN-M-` prefix.

> **Verdict derivation**:
> - Any R-row FAIL in 5.2 → **Auto-reject**
> - Any M-row TRIGGERED in 5.3 → **Manual review** (if no auto-reject)
> - All A-rows PASS in 5.2, no M-rows triggered → **Auto-approve**

---

## 6. Monitoring & Observability

**Reviewer: SRE**

### 6.1 Logging (mandatory)

| Field | Value |
|-------|-------|
| **Log Retention** | `<7 / 14 / 21 days>` |
| **Sourcetype** | `<value>` |
| **PII Redaction Confirmed** | `<Yes / No>` |

> **Validation Rule**: PII redaction value must match the PII classification from Section 5.
> If PII = Elevated or Yes, redaction must be confirmed.

### 6.2 Custom Alerts (mandatory - at least one)

| Alert Name | Metric | Threshold | Severity |
|-----------|--------|-----------|----------|
| `<alert-name>` | `<metric>` | `<threshold>` | `<P1 / P2 / P3>` |
| `<alert-name>` | `<metric>` | `<threshold>` | `<severity>` |

### 6.3 Required Instrumentation

| Instrumentation | Implemented |
|----------------|-------------|
| **Tracing** | `<Yes / No / Planned>` |
| **Metrics** | `<Yes / No / Planned>` |
| **Logs** | `<Yes / No / Planned>` |

---

## 7. Agent Decision Overrides

> This section is **auto-populated by the CyTech Agent Reviewer** during the review.
> Do not fill in manually.

### 7.1 DevOps Agent

| Field | Value |
|-------|-------|
| Resolved Cluster - QA | *(auto)* |
| Resolved Cluster - INT | *(auto)* |
| Resolved Cluster - STG | *(auto)* |
| Resolved Cluster - PROD | *(auto)* |
| Assignment Rationale | *(auto)* |

### 7.2 IT Agent

| Dependency # | Status | Action | Jira Ticket |
|-------------|--------|--------|-------------|
| 1 | *(auto: open / pending / blocked)* | *(auto)* | *(auto or N/A)* |
| 2 | *(auto: open / pending / blocked)* | *(auto)* | *(auto or N/A)* |

### 7.3 Security Agent

| Field | Value |
|-------|-------|
| Decision | *(auto: auto-approve / manual-review / review-meeting / reject)* |
| Reason | *(auto)* |
| Reviewer (if manual) | *(auto or N/A)* |

### 7.4 SRE Agent

| Field | Value |
|-------|-------|
| Decision | *(auto: auto-approve / reject)* |
| Monitoring Jira Task | *(auto: link or N/A)* |
| Reason (if reject) | *(auto)* |

---

## 8. Final Approvals

| Reviewer | Domain | Approved (Y/N) | Date | Comments |
|----------|--------|:--------------:|------|----------|
| Security | | | | |
| IT | | | | |
| SRE | | | | |

---

## Version History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | `<date>` | `<author>` | Initial plan |
